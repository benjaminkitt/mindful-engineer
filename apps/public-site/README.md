# Mindful Engineer Public Site

This Astro app is the standalone public reading surface for `mindful.engineer`.

## Commands

- `npm install`
- `npm run dev`
- `npm run build`
- `npm run deploy`

## Deployment

The app builds to `dist/` and is configured for Cloudflare Pages via [`wrangler.toml`](./wrangler.toml).
